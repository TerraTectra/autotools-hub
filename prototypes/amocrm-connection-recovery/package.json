{
  "name": "amocrm-connection-recovery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/core.js && node --check src/index.js && node --test"
  },
  "engines": { "node": ">=20" }
}

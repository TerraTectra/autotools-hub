diff --git a/node/utxo_db.py b/node/utxo_db.py
--- a/node/utxo_db.py
+++ b/node/utxo_db.py
@@ -1441,21 +1441,13 @@ class UtxoDB:
         conn = self._conn()
         try:
             now = int(time.time())
-            scan_limit = min(
+            page_size = min(
                 MAX_POOL_SIZE,
                 max_count * MAX_MEMPOOL_CANDIDATE_SCAN_FACTOR,
             )
-            rows = conn.execute(
-                """SELECT tx_id, tx_data_json FROM utxo_mempool
-                   WHERE expires_at > ?
-                   ORDER BY fee_nrtc DESC
-                   LIMIT ?
-                """,
-                (now, scan_limit),
-            ).fetchall()
             candidates = []
             stale_tx_ids = []
             selected_spend_inputs = set()
             selected_data_inputs = set()
-
-            for row in rows:
+            scanned = 0
+            cursor = None
+
+            def consider_row(row):
                 tx_id = row['tx_id']
                 try:
                     tx = json.loads(row['tx_data_json'])
@@ -1464,11 +1456,11 @@ class UtxoDB:
                         tx.get('data_inputs', [])
                     )
                 except Exception:
                     stale_tx_ids.append(tx_id)
-                    continue
+                    return
 
                 if not input_ids or data_inputs is None:
                     stale_tx_ids.append(tx_id)
-                    continue
+                    return
 
                 for box_ids in (input_ids, data_inputs):
                     if not box_ids:
@@ -1482,7 +1474,7 @@ class UtxoDB:
                     ).fetchone()['n']
                     if unspent_count != len(set(box_ids)):
                         stale_tx_ids.append(tx_id)
-                        break
+                        return
                 else:
                     input_set = set(input_ids)
                     data_input_set = set(data_inputs)
@@ -1498,14 +1490,65 @@ class UtxoDB:
                         or data_input_set & selected_spend_inputs
                     ):
-                        continue
+                        return
 
                     candidates.append(tx)
                     selected_spend_inputs.update(input_set)
                     selected_data_inputs.update(data_input_set)
-                    if len(candidates) >= max_count:
-                        break
+
+            # Resolve conflicts while paging through the deterministic fee order.
+            # A fixed pre-filter LIMIT lets one conflict cluster monopolize the
+            # scan window and starve compatible transactions below it. Keyset
+            # pagination keeps the work bounded by MAX_POOL_SIZE without relying
+            # on OFFSET while the mempool can change concurrently.
+            while len(candidates) < max_count and scanned < MAX_POOL_SIZE:
+                limit = min(page_size, MAX_POOL_SIZE - scanned)
+                if cursor is None:
+                    rows = conn.execute(
+                        """SELECT tx_id, tx_data_json, fee_nrtc, submitted_at
+                           FROM utxo_mempool
+                           WHERE expires_at > ?
+                           ORDER BY fee_nrtc DESC, submitted_at ASC, tx_id ASC
+                           LIMIT ?
+                        """,
+                        (now, limit),
+                    ).fetchall()
+                else:
+                    fee_nrtc, submitted_at, tx_id = cursor
+                    rows = conn.execute(
+                        """SELECT tx_id, tx_data_json, fee_nrtc, submitted_at
+                           FROM utxo_mempool
+                           WHERE expires_at > ?
+                             AND (
+                               fee_nrtc < ?
+                               OR (
+                                 fee_nrtc = ?
+                                 AND (
+                                   submitted_at > ?
+                                   OR (submitted_at = ? AND tx_id > ?)
+                                 )
+                               )
+                             )
+                           ORDER BY fee_nrtc DESC, submitted_at ASC, tx_id ASC
+                           LIMIT ?
+                        """,
+                        (
+                            now,
+                            fee_nrtc,
+                            fee_nrtc,
+                            submitted_at,
+                            submitted_at,
+                            tx_id,
+                            limit,
+                        ),
+                    ).fetchall()
+
+                if not rows:
+                    break
+
+                scanned += len(rows)
+                last_row = rows[-1]
+                cursor = (
+                    last_row['fee_nrtc'],
+                    last_row['submitted_at'],
+                    last_row['tx_id'],
+                )
+
+                for row in rows:
+                    consider_row(row)
+                    if len(candidates) >= max_count:
+                        break
+
+                if len(rows) < limit:
+                    break
 
 
             for tx_id in stale_tx_ids:
